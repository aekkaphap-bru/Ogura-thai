<nav class="navbar header-top fixed-top navbar-expand-lg  navbar-dark " id="topbarwhite" ><!--style="max-height:60px;"-->
    <a class="navbar-brand" href="#">
        <img src="http://octap01/intranet/image/logo2022.png" height="40" alt="OCT Web Applications">
        OCT Web Applications 
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText"
            aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav ml-md-auto d-md-flex text-left">
            <li class="nav-item">

            </li>
        </ul>
        <ul class="navbar-nav ml-md-auto d-md-flex">
            <li class="nav-item dropdown no-arrow">
                <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <span class="mr-2 d-none d-lg-inline text-white">
                        @using Microsoft.AspNet.Identity
                        @if (Session["UserName"] != null)
                        { @Session["FistNameLastName"]  }
                        else
                        { <b>You are not logged</b> }
                    </span>
                    <img class="img-profile rounded-circle" src="~/static/img/undraw_profile.svg" style="height: 35px;">
                </a>
                <!-- Dropdown - User Information -->
                @if (Session["UserName"] != null)
                {
                    <div class="dropdown-menu dropdown-menu-right shadow animated--grow-in" aria-labelledby="userDropdown">
                        <a class="dropdown-item" href="/Account/Manage/">
                            <i class="fas fa-cogs fa-sm fa-fw mr-2 text-gray-400"></i>
                            Change Password
                        </a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="#" data-toggle="modal" data-target="#logoutModal">
                            <i class="fas fa-sign-out-alt fa-sm fa-fw mr-2 text-gray-400"></i>
                            Logout
                        </a>
                    </div>
                }
               
            </li>
            @if (Session["UserName"] != null)
            {
             <li class="nav-item">
                <a class="nav-link" href="#" data-toggle="modal" data-target="#logoutModal">
                    <i class="fas fa-sign-out-alt fa-lg fa-fw mr-2 text-white"></i>
                    <span class="text-white" style="vertical-align: middle;">Logout </span>
                </a>
            </li>
            }
            else
            {
                <li class="nav-item">
                <a class="nav-link" href="/" >
                    <i class="fas fa-sign-in-alt fa-lg fa-fw mr-2 text-white"></i>
                    <span class="text-white" style="vertical-align: middle;">Login </span>
                </a>
            </li>
            }
            
        </ul>
    </div>
</nav>